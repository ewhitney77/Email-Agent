# Core scraping + matching
requests>=2.31
beautifulsoup4>=4.12
numpy>=1.24

# Free, local embedding backend (default). Pulls in torch.
sentence-transformers>=2.2

# JavaScript-rendered career pages (fallback). After install run:
#   playwright install chromium
playwright>=1.40

# Optional: OpenAI embedding backend. Only needed if EMBEDDING_BACKEND=openai
# openai>=1.0
